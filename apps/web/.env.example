# Sanity project the storefront reads from. If unset, vite.config.ts falls
# back to examples/davids-bridal/.env so the demo picks up the same project
# the migration pipeline writes to.
#
# SANITY_PROJECT_ID=your-project-id
# SANITY_DATASET=production
